@model DetailViewModel

<div class="row">
    @foreach (var ticket in Model.OpenTickets)
    {
        <div class="col-sm-4">
            <partial name="_TicketCard" model="ticket" />
        </div>
    }
</div>
